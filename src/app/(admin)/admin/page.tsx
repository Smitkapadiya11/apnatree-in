export default function AdminHomePage() {
  return (
    <section className="mx-auto max-w-5xl space-y-6">
      <div>
        <p className="text-muted-foreground text-xs tracking-[0.35em] uppercase">Mission Control</p>
        <h1 className="font-[family-name:var(--font-heading)] text-4xl tracking-tight sm:text-5xl">
          Allocate trees. Publish proof. Ship honour.
        </h1>
      </div>
      <p className="text-muted-foreground max-w-3xl text-lg leading-relaxed">
        Nested CRUD surfaces land in Phase&nbsp;4 — routing, RBAC middleware, and audit-ready typography are live today.
      </p>
    </section>
  );
}
