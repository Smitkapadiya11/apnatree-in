import { MarketingPlaceholder } from "@/components/shared/MarketingPlaceholder";

export default function AdminUsersPage() {
  return (
    <MarketingPlaceholder
      title="Renters & stewards"
      description="Role-aware directory with impersonation safeguards — surfaces once audit trails + SOC-friendly exports finalize."
    />
  );
}
