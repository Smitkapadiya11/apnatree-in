import { MarketingPlaceholder } from "@/components/shared/MarketingPlaceholder";

export default function AdminContractsPage() {
  return (
    <MarketingPlaceholder
      title="Contracts & allocations"
      description="PREBOOKING_PAID → AWAITING_CONFIRMATION workflows with automated Phase 2 Stripe invoices — wired after webhook hardening + audit logs."
    />
  );
}
