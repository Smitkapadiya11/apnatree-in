import { MarketingPlaceholder } from "@/components/shared/MarketingPlaceholder";

export default function HarvestMarketingPage() {
  return (
    <MarketingPlaceholder
      title="Harvest with receipts"
      description="Every crate ships with variety proof, gross weight, grading, and courier telemetry — mirrored inside HarvestOrder records once dashboards unlock Phase 3."
    />
  );
}
