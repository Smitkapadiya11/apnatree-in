import { MarketingPlaceholder } from "@/components/shared/MarketingPlaceholder";

export default function PricingPage() {
  return (
    <MarketingPlaceholder
      title="Transparent INR economics"
      description="Every rupee is computed server-side via calculatePricing — multi-year stewardship incentives included. Stripe secures Phase 1 deposits through checkout sessions wired in Phase 2."
    />
  );
}
