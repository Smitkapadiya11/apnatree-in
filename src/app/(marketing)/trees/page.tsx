import { MarketingPlaceholder } from "@/components/shared/MarketingPlaceholder";

export default function TreesPage() {
  return (
    <MarketingPlaceholder
      title="Reserve the tier that fits your story"
      description="Three distinct ages of Kesar canopy — each mapped to transparent pre-booking deposits, annual stewardship rent, and scientifically projected yields."
    />
  );
}
