import { MarketingPlaceholder } from "@/components/shared/MarketingPlaceholder";

export default function DashboardProfilePage() {
  return (
    <MarketingPlaceholder
      title="Profile & jurisdiction"
      description="Addresses influence customs paperwork for overseas crates — react-hook-form + zod enforcement arrives when onboarding wizard expands beyond OAuth baseline."
    />
  );
}
