import { MarketingPlaceholder } from "@/components/shared/MarketingPlaceholder";

export default function DashboardPaymentsPage() {
  return (
    <MarketingPlaceholder
      title="Payments ledger"
      description="Phase 1 deposits, annual stewardship invoices, and shipping settlements reconcile here using immutable Stripe references mirrored in RentPayment."
    />
  );
}
